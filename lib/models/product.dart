class Product {
  String? pId, pName, pPrice, pDescript, pLocation, pCategory;
  int pQty;
  Product({this.pId,
      this.pName,
      this.pPrice,
      this.pCategory,
      this.pDescript,
      this.pLocation,
      this.pQty=1});
}
